
task javadocJar(type: Jar, dependsOn: 'javadoc') {
    from javadoc.destinationDir
    destinationDir = file 'jar-builds'
}

jar { 
   	destinationDir = file 'jar-builds'
}

task deobfJar(type: Jar) {
    from sourceSets.main.output
    destinationDir = file 'jar-builds'
    classifier = 'dev'
}

task sourcesJar(type: Jar) {
    from sourceSets.main.java
    destinationDir = file 'jar-builds'
    classifier = 'src'
}

artifacts {
    archives jar
    archives sourcesJar
    archives deobfJar
}
